# 使用Node.js作为基础镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制package.json和package-lock.json
COPY package*.json ./

# 安装依赖
RUN npm install --production

# 复制代理服务器代码
COPY proxy.js ./

# 暴露端口
EXPOSE 3000

# 启动代理服务器
CMD ["node", "proxy.js"]